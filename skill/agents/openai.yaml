interface:
  display_name: "Dokki Slides"
  short_description: "Create interactive slides and editable PPTX files"
  brand_color: "#635BFF"
  default_prompt: "Use $dokki-slides to turn my source material into an interactive slide deck with an editable PPTX export."
policy:
  allow_implicit_invocation: true

